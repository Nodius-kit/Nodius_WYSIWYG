// JSDOM test environment setup
// Vitest handles JSDOM setup via vitest.config.ts environment: 'jsdom'
// This file is for any global test configuration

import { expect } from 'vitest';

// Custom matchers can be added here if needed
